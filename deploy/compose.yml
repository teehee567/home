services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    image: noob-server:dev
    container_name: noob-server
    ports:
      - "4433:4433/udp"
    volumes:
      - ${NOOB_DATA_DIR:?run via deploy/redeploy.ps1 so NOOB_DATA_DIR is set}:/data
    restart: unless-stopped
